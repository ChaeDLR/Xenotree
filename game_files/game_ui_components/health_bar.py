# This is where we will create the health bar class
# we're making it a class because classes are passed by reference so we can act on it from anywhere
# and it will change all instances across all levels
from pygame.surface import Surface